# I have created project with the name dj_project
# then I have created two app with names app1, and users
# users app will manged only authentications
    in this users app i have created 3 views
    -- Register view(using this users can Register)
    -- Login view(using this users can Login)
    -- Logout view(users can Logout here..)
# app1 usefull manging the dashbord
     in this app1 app i have created couple of views
     -- index (once user logged in index view will be render)
     -- submit_asset (users can able to submit to their assets)
     -- dashboard_live_data (this views render the live up to data)

